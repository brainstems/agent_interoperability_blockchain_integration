version: "3.8"
#claude-3-5-haiku-20241022
#claude-3-5-haiku-20241022
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    image: marketing-crew/api:latest
    ports:
      - "5001:5001"
    environment:
      - REDIS_URL=${REDIS_URL}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - LLM_MODEL=claude-3-5-haiku-20241022
    networks:
      - crew_network

  # inventory-worker:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.inventory
  #   image: marketing-crew/inventory:latest
  #   volumes:
  #     - ./logs/inventory:/app/logs
  #   environment:
  #     - OPENAI_API_KEY=${OPENAI_API_KEY}
  #     - REDIS_URL=${REDIS_URL}
  #     - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
  #     - LLM_MODEL=claude-3-5-haiku-20241022
  #   networks:
  #     - crew_network

  # promotional-worker:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.promotional
  #   image: marketing-crew/promotional:latest
  #   volumes:
  #     - ./logs/promotional:/app/logs
  #   environment:
  #     - OPENAI_API_KEY=${OPENAI_API_KEY}
  #     - REDIS_URL=${REDIS_URL}
  #     - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
  #     - LLM_MODEL=claude-3-5-haiku-20241022
  #   networks:
  #     - crew_network

  simulation-worker:
    build:
      context: .
      dockerfile: Dockerfile.simulation
    image: marketing-crew/simulation:latest
    volumes:
      - ./logs/simulation:/app/logs
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - REDIS_URL=${REDIS_URL}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - LLM_MODEL=claude-3-5-haiku-20241022
    networks:
      - crew_network

  # inventory-execution-worker:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.inventory_execution
  #   image: marketing-crew/inventory-execution:latest
  #   volumes:
  #     - ./logs/inventory-execution:/app/logs
  #   environment:
  #     - OPENAI_API_KEY=${OPENAI_API_KEY}
  #     - REDIS_URL=${REDIS_URL}
  #     - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
  #     - LLM_MODEL=claude-3-5-haiku-20241022
  #   networks:
  #     - crew_network

  # promotion-crew:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.promotion
  #   image: marketing-crew/promotion:latest
  #   volumes:
  #     - ./logs/promotion:/app/logs
  #   environment:
  #     - OPENAI_API_KEY=${OPENAI_API_KEY}
  #     - REDIS_URL=${REDIS_URL}
  #     - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
  #     - LLM_MODEL=claude-3-5-haiku-20241022
  #   networks:
  #     - crew_network

  # corn-flakes-worker:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.corn_flakes
  #   image: marketing-crew/corn-flakes:latest
  #   volumes:
  #     - ./logs/corn-flakes:/app/logs
  #   environment:
  #     - OPENAI_API_KEY=${OPENAI_API_KEY}
  #     - REDIS_URL=${REDIS_URL}
  #     - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
  #     - LLM_MODEL=claude-3-5-haiku-20241022
  #   networks:
  #     - crew_network

  # promotional-swarm:
  #   build:
  #     context: promotional-swarm
  #     dockerfile: Dockerfile
  #   networks:
  #     - crew_network
  #   environment:
  #     - REDIS_URL=${REDIS_URL}
  #     - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
  #     - LLM_MODEL=claude-3-5-haiku-20241022

  # inventory-swarm:
  #   build:
  #     context: inventory-swarm
  #     dockerfile: Dockerfile
  #   networks:
  #     - crew_network
  #   environment:
  #     - REDIS_URL=${REDIS_URL}
  #     - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
  #     - LLM_MODEL=claude-3-5-haiku-20241022

  # product-agent-swarm:
  #   build:
  #     context: product-agent-swarm
  #     dockerfile: Dockerfile
  #   networks:
  #     - crew_network
  #   environment:
  #     - REDIS_URL=${REDIS_URL}
  #     - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
  #     - LLM_MODEL=claude-3-5-haiku-20241022

  marketing-crew:
    build:
      context: .
      dockerfile: Dockerfile.marketing
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - REDIS_URL=${REDIS_URL}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - LLM_MODEL=claude-3-5-haiku-20241022
    networks:
      - crew_network

networks:
  crew_network:
    driver: bridge
